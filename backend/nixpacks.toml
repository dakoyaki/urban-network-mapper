[phases.setup]
nixPkgs = ["python311", "gcc", "postgresql", "gdal", "gcc", "gxx", "binutils"]

[phases.install]
cmds = [
  "pip install --upgrade pip",
  "pip install --no-cache-dir -r requirements.txt"
]

[start]
cmd = "gunicorn -c gunicorn.conf.py wsgi:app"

